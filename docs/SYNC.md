# Sync

Install truth: root [`install.sh`](../install.sh).  
Deploy copy: `site/install.sh` (must stay byte-identical — `make -C .. sync-install` from repo root, or `cmp install.sh site/install.sh`).
