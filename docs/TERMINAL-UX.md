# Terminal UX

Modern default = themed chrome (README). Scripts use `--core`.

Layout cues: type gutter, icons, table chrome, match highlight. Off under `--core` and `NO_COLOR`.

Ship narrative: [README](../README.md) · [f00.sh](https://coreutils.f00.sh).
