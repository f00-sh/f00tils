# GNU flag compliance

**Not the product contract.** Ship contract is [README](../README.md) common-track `--core` + modern default.

This file was a 1.9k-line flag encyclopedia. Removed in the Elon optimization pass to stop dual-truth.

For flag depth work: open an issue against a specific tool; prove with `asm/benches/parity.sh` or a focused battery on shipped `f00-*`.
